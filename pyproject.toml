[project]
name = "ocr"
version = "0.1.0"
description = "OCR project using mlx-vlm"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mlx-vlm>=0.3.9",
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "PyMuPDF",
]

[tool.uv]
dev-dependencies = []
